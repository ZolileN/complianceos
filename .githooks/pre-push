#!/usr/bin/env bash
# Local quality gate — acts as CI while GitHub Actions is unavailable.
# Skip in emergencies with: git push --no-verify
set -euo pipefail

echo "[pre-push] lint"
npm run lint

echo "[pre-push] typecheck"
npm run typecheck

echo "[pre-push] tests"
npm test

echo "[pre-push] all checks passed"
